// berat

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(){
	int t;
	scanf("%d", &t);
	for (int i=0; i<t; i++){
		char a[10000], b[10000];
		scanf(" %s", a);
		scanf(" %s", b);
		int digit = strlen(a);
		for (int j=0; j<2; j++){
			save = 0;
			for (int k=0; k<=digit; k++)
		}
	}
}