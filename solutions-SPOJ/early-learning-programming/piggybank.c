#include <stdio.h>
int main()
{
    int t;
    scanf("%d", &t);
    for (int i=0; i<t; i++){
        double e, f;
        scanf("%lf%lf", &e, &f);
        double bb = f - e;
        int n;
        scanf("%d", &n);
        for (int j=0; j<n; j++){

        }
    }
}
